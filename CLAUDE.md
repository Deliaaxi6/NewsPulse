# CLAUDE.md — NewsPulse 快速定位指南

> 本文件是 AI 协作者的首要入口。新会话请先读本文件，需要更多细节时再读 ROADMAP.md 与 docs/。

## 项目一句话

每日新闻情绪驱动的全自动学习型量化模拟交易系统（10万模拟资金，不动实盘）。

## 黄金准则（每次改动前必读）

1. **100元实盘本金是红线**：本项目所有交易均为模拟，禁止任何真实资金操作
2. **学生合规红线**：不推荐他人投资、不碰外汇/二元期权、不发布任何投资建议
3. **密钥管理**：secrets 存服务器 `secrets.json`（chmod 600），禁止进 git、禁止硬编码、禁止出现在日志
4. **不改动用户 AGENTS.md 规则**：邮件通知（规则15-17）、杀进程需确认（规则28）、禁止无过滤终止进程（规则27）必须遵守
5. **边界安全**：熔断机制是核心安全阀，改动决策逻辑时必须同步评估熔断触发条件

## 快速命令

```bash
# 本地手动运行（开发）
python run_all.py

# 单模块运行
python src/fetch_news.py --date 2026-08-10
python src/filter_news.py --date 2026-08-10
python src/decision.py --date 2026-08-10
python src/sim_account.py --date 2026-08-10
python src/daily_report.py --date 2026-08-10

# 静态检查（改动后必跑）
python -m compileall src/
ruff check src/
```

## 数据目录结构（约定）

```
quant_news/
├── src/                  # 模块源码
│   ├── fetch_news.py     # ① 新闻爬取
│   ├── filter_news.py    # ② 关键词筛选+情绪
│   ├── decision.py       # ③ 决策+杠杆
│   ├── sim_account.py    # ④ 模拟撮合
│   ├── daily_report.py   # ⑤ 报告+邮件
│   ├── config.py         # 配置（不含密钥）
│   └── run_all.py        # 一键入口
├── data/                 # CSV 数据（git 忽略）
├── reports/              # HTML 报告（git 忽略）
├── logs/                 # 运行日志（git 忽略）
├── docs/                 # 文档
├── tests/                # 测试（阶段1为日志断言脚本）
├── secrets.json          # 密钥（仅服务器，不进 git）
├── requirements.txt
└── ROADMAP.md / PROGRESS.md / CLAUDE.md
```

## 与用户 AGENTS.md 的协作约定

- 任务完成必须发邮件通知（规则15），出错发错误邮件（规则16）
- 需要用户确认时先发权限邮件（规则17，message 固定为"权限！！！"）
- 杀进程/停止服务前必须先列 PID + 发权限邮件 + question 确认（规则28）
- 修改代码前必须先阅读相关文件上下文（规则1）、说明修改计划（规则3）
- 每次改动 ≤5 个文件（规则8），超出则分步执行

## 常见坑

1. akshare 接口可能变更 → 爬取失败时检查重试与备用接口逻辑，不要假设接口永久可用
2. 非交易日新闻为空 → 这是正常行为，不是 bug（检测空数据即跳过）
3. 前 20 天预热期不交易 → 不要误判为故障
4. 熔断后自动恢复有冷却条件 → 修改恢复逻辑时必须保留防循环熔断设计
5. Windows 本地与 Linux 服务器路径差异 → 用 config.py 统一管理路径，禁止硬编码

## 领域背景（快速速览）

- 交易规则：信号次日开盘价成交、T+1、佣金万2.5（最低5元）、卖出印花税0.05%、单票≤30%、总仓≤90%、涨停不卖/跌停不买
- 杠杆：情绪分≥0.6→3倍、0.3~0.6→2倍、<0.3→1倍
- 熔断：3倍杠杆市值跌超33%→清仓+邮件告警+停交易；冷却5日+连续2日情绪转正→1倍杠杆恢复
- 数据源：东财/新浪新闻（akshare 接口），备用接口新浪